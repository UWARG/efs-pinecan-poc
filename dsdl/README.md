A description of the folders here, how to gen dsdl, and where to add custom

I think probably rename the submodule folders I just picked random names for now

the makefile sets up a venv and runs the generate scripts, with the generated files in ./generated/
